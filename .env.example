# Chave da API do Giphy (developers.giphy.com, gratuita). Sem isso, cai de
# volta na chave pública de teste do próprio Giphy (rate limit bem baixo,
# só serve pra dev/demo) - ver src/lib/giphy.ts.
VITE_GIPHY_API_KEY=
